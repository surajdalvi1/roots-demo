// import the wrapper
contentstack = require ‘roots-contentstack’
# other roots declarations / definitions
module.exports =
	extensions: [
		contentstack
		# we’ll first get all the required config info
			api_key: '',
			access_token: '',
			environment: '',
			content_types:
				partials:
					# multiple
					uid: 'partial content type uid',
					template: 'path',
					## anything else?
					# lang: [‘en-us’, ‘fr-fr’]
					#optional
					entry_uid: ‘’,
					filter: 'query'
					path: (obj) ->
						# defined path or from key
				pages:
					# multiple
					uid: 'partial content type uid',
					template: 'path',
					## anything else?
					# lang: [‘es-es’]
					# optional
					entry_uid: ‘’,
					filter: 'query'
					path: (obj) ->
						# defined path or from key
				# lang: [‘en-us’, ‘fr-fr’]